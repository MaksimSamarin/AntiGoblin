{
  "routing": {
    "domainStrategy": "IPIfNonMatch",
    "rules": [
      {
        "type": "field",
        "inboundTag": [
          "proxy-relay-ss"
        ],
        "outboundTag": "vless-reality"
      },
      {
        "type": "field",
        "inboundTag": [
          "redirect"
        ],
        "outboundTag": "direct"
      }
    ]
  }
}
